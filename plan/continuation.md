# Continuation — Single Current Task

## Protocol

- **Purpose:** the only active task for the current dispatch.
- **Authority:** current-task execution only.
- **Must contain:** one roadmap node ID, why now, dependencies, output files, local context, scope boundary, success condition, verification, stress test, audit target.
- **Must not contain:** multiple queued tasks, backlog items, roadmap-wide planning, long history.
- **Update rule:** after each commit, update this file to reflect the current active task state.
- **Update rule:** replace this file with the next single task only when the current task is completed or intentionally split.
- **Update rule:** if unfinished and still the same task, keep the same node ID and refresh only the local context/state as needed.

## Task Identity

- **Node ID:** GT14
- **Title:** GitHub template packaging
- **Status:** READY

## Why now

GT13 is complete — end-to-end smoke test passes (29/29 assertions), pre-commit/pre-merge-commit hooks implemented, all GT7–GT12 suites still green (133 total). Both GT14 dependencies are met: GT2 (initializer UX and idempotence contract) and GT13 (integration gate). GT14 is the packaging node that makes the scaffold repo usable as a GitHub template before fresh-repo acceptance (GT15).

## Dependencies

- GT2 output: initializer UX contract (D28–D31)
- GT13 output: proven spine (smoke test, pre-commit hooks, all component tests)
- GT3 output: `init.sh` creates membrane topology
- GT6 output: `bootstrap.sh` activates governed environment

## Output Files

- `README.md` — template README: pre-init vs post-init state, quick-start instructions
- `.github/` — any template-specific GitHub configuration (if needed)
- `continuation.md` (refresh state while GT14 remains active)

## Local Context

- GT14 is an H node. The deliverable is packaging, not new enforcement logic.
- The template must work with GitHub's "Use this template" flow: a new repo is generated containing only the scaffold/template-authoring content.
- Per D32: the published template remote must expose NO membrane branches (`now`, `meta`, `provenance/scaffold`) and no `refs/membrane/root`. Those are created by `init.sh` after template generation.
- The README must guide a new operator from "I just generated this repo" to "governed now branch" without requiring knowledge of design internals.
- The current README.md (if any) on the main branch is a scaffold placeholder. It needs to be replaced with the template-facing README.
- Optional GitHub CLI path should be mentioned as an alternative to the web "Use this template" button.
- The template should include init.sh on the main branch (it's already there). bootstrap.sh is generated by init.sh onto the now branch, so it doesn't need to be in the template.
- Enforcement scripts (.now/src/*, .now/hooks/*) live on the main branch as source but are installed onto the now branch by the smoke test / future packaging. The template should include them so init.sh + bootstrap can reference them.

## Scope Boundary

In scope:
- Template README (pre-init → post-init guidance)
- Repository settings/docs for "Use this template" flow
- Verify published template would expose no membrane branches or refs
- Optional GitHub CLI path documentation
- Warning that template generation alone does not produce the governed topology

Out of scope:
- New enforcement logic or constraint changes
- Fresh-repo acceptance testing (GT15)
- Release notes or hardening (GT16)

## Success Condition

- A new operator can generate a repo from the template and understand the next command without reading design internals.
- Packaging docs describe only workflows that actually exist in the implemented spine.
- The published template remote would expose no membrane branches or refs (verified by inspection).

## Stress Test

- Does the README make sense to someone who has never seen the repo?
- Are the documented steps actually correct (init.sh → bootstrap.sh → governed)?
- Does the template include everything init.sh needs to run?
- Would a generated repo contain any membrane branches or refs?

## Audit Target

- README.md exists and covers pre-init → post-init flow
- No membrane branches exist on the main/default branch
- init.sh is present and executable in the template
- Documented steps match the actual implemented flow
- D32 constraint satisfied (no membrane refs in template)

## Verification

- Manual inspection of README for clarity and correctness
- `git branch --list` on the template repo shows no membrane branches
- `git show-ref` shows no `refs/membrane/root`
- Documented commands (`./init.sh`, `./bootstrap.sh`) match implemented scripts
